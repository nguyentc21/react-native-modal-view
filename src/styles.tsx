import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  modalWrapContainer: {
    height: '100%',
    width: '100%',
    backgroundColor: 'transparent',
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,
  },
});
